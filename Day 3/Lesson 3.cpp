#include<iostream>
#include<cmath>
#include<fstream>
#include<vector>
#include<algorithm>
void matrix_fill(std::vector<std::vector<int> > &);
int main(){
	std::vector<std::vector<int> > vec (10,10);
	matrix_fill(vec);
	return 0;
}
#void matrix_fill(std::vector<std::vector<int> > & x){
#	for(int i = 0; i < )
#}
